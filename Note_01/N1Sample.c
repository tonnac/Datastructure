#include "SampleDeque.h"

int main()
{
	//STACK s1;
	//initial(&s1);
	//empty(&s1);
	//for (int i = 0; push(&s1, i + 1); i++)	{}
	//empty(&s1);
	//Data temp = get(&s1);
	//Print(&s1);
	//for (;!empty(&s1);) 
	//{
	//	temp = pop(&s1);
	//}
	//empty(&s1);

	//QUEUE q1;
	//initial(&q1);

	//empty(&q1);

	//for (int i = 0; i<10; i++)	
	//{
	//	push(&q1, i + 1);
	//}
	//get(&q1);
	//Print(&q1);
	//Data temp;
	//for (;!empty(&q1);) 
	//{
	//	temp = pop(&q1);
	//}

	//empty(&q1);

	//QARR qa1;
	//Qinitial(&qa1);
	//Qempty(&qa1);
	//for (int i = 0; Qpush(&qa1, i + 1); i++) {}
	//Data temp = Qget(&qa1);
	//QPrint(&qa1);
	//for (int i = 0; i < 5; i++)
	//{
	//	temp = Qpop(&qa1);
	//}
	//QPrint(&qa1);
	//for (int i = 0; i < 3; i++)
	//{
	//	Qpush(&qa1, i + 15);
	//}
	//QPrint(&qa1);
	//for (int i = 0; i<6;i++)
	//{
	//	temp = Qpop(&qa1);
	//}
	//Qempty(&qa1);

	//DEQUE dq;


	//initial(&dq);

	//empty(&dq);
	//pushback(&dq, 3);
	//pushback(&dq, 5);
	//pushfront(&dq, 1);
	//pushfront(&dq, 8);
	//pushfront(&dq, 9);
	//pushback(&dq, 7);
	//for (int i = 0; i < 6; i++)
	//{
	//	popback(&dq);
	//}

	//empty(&dq);

	ADEQUE a1;

	initial(&a1);

	Aempty(&a1);
	Apushfront(&a1, 3);
	Apushfront(&a1, 4);
	Apushfront(&a1, 5);
	Apushfront(&a1, 6);

	APrint(&a1);

	return 0;
}